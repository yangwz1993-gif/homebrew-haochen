# haochen v0.1.2 · 修复 TODO（产品与用户已确认）

> 发给：研发（kimi） ｜ 来自：产品（haochen） ｜ 时间：2026-08-26
> 目标：交付**可用**的 haochen 包/应用，解决下列全部问题 + 其它体验问题，最终打一个合规的 **v0.1.2**。
> 顺序原则（已纠正）：**先修好 + 验证 → 版本管理落地 → 最后才打 v0.1.2**。不要再"未修就打包"。

---

## 1. 权限检测 bug（高优，反复多次）

- **问题**：点「我已授权」后，即便系统设置里辅助功能 + 屏幕录制**都已开启（开关蓝）**，App 仍提示"还未全部授权"、弹窗不消失。即便签名已稳定（haochen Local Signing）仍出现。
- **要做**：修权限检测/刷新逻辑。stable 签名 + 权限已开时，点「我已授权」应**正确检测到**两个权限并用**关闭弹窗**。请排查 `permissions` / `app_shell` 的检测与"我已授权"回查路径，为何读不到已授权的 TCC 状态。

## 2. 打包/安装质量问题（高优）

- **问题**：打开 `haochen-0.1.1.dmg` 后，`haochen.app` 不是正式安装形态——点开像是"临时程序"，**应用程序列表（Lauchpad/Applications）查不到它**。疑似打包链问题（此前由产品手动重签 + hdiutil 重打 dmg，可能引入问题，或 build 产物不完整）。
- **要做**：**由研发用正式 `build.sh` 打包**（含正确签名链、PyInstaller 资源、CFBundleIdentifier/LSUIElement、版本号），确保 `.dmg` 挂载后 ①`haochen.app` 拖进 Applications 能被系统正式识别 ②双击可正常启动 ③应用列表可见。**不要再由产品手动重签/重打 dmg。**

## 3. 干净安装首启报"授信失败"（需最终验证）

- **问题**：干净安装（真实用户环境无 `~/Library/Application Support/haochen/signing/haochen-signing.pem`）启动报"授信失败（读文件失败）"。
- **要做**：确认修复到位——首启**不再读用户环境里不存在的 pem**；已稳定签名 app 不触发授信；干净安装（无 signing 目录）正常启动、不弹"授信失败"。（v0.1.1 已修，需最终验证。）

## 4. 版本管理流程落地（框架已建，需固定）

- **要做**：固定流程 = 卸载 → 修 → 打包 → **版本号 +1** → 写 `docs/CHANGELOG.md` → 旧版归档到 `packaging/dist/archive/`。本次 v0.1.1 → **v0.1.2**，记录完整版本历史（版本号/变更/产物/时间/状态）。

## 5. 其他体验问题

- 研发在修复第 1/2 项时，顺带自查并解决其它影响"开箱即用 / 商业级 / 体验完美"的问题（读屏、看图识人、窗口、气泡、崩溃恢复等），无一票否决项残留。

---

## 验收门槛（全部通过才算可交付 v0.1.2）

1. 权限检测：stable 签名 + 权限已开 → 点「我已授权」→ 弹窗关闭、不再反复。
2. 打包：`v0.1.2.dmg` 挂载后 app 被系统正式识别、拖进 Applications 可见、双击正常启动。
3. 干净安装：首次启动不报"授信失败"，可引导授权、正常用。
4. 版本管理：CHANGELOG 有 v0.1.2 完整记录，旧版归档。
5. 功能与体验：读屏/看图识人可用，无致命/阻断问题（回归 PASS=7 FAIL=0）。

> 请按 1→2→3→4→5 顺序，修复+验证后**由研发用 build.sh 打包 v0.1.2**，附证据交付，产品/用户终审。
