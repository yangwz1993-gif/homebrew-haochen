# haochen 0.3.2

这是一次围绕“像桌面伙伴一样自然说话”的完整体验升级。短回答不再常驻占住桌面，而是以轻量漫画气泡出现；需要时可以一键展开完整会话。与此同时，首启称呼、长期用户档案和自定义 OpenAI 兼容模型形成了可用的完整闭环。

## 主要变化

- 每条短结果默认显示 20 秒；鼠标停在气泡内会暂停计时，移开后继续剩余时间，新结果不会继承上一条的余额。
- 短气泡提供小型关闭、发送和展开图标；展开完整会话时保留尚未发送的草稿。
- haochen 的唯一名字固定为小写英文 `haochen`，没有中文名；只有经过用户确认的称呼才会写入长期档案并注入对话。
- 首启和设置页均可配置 OpenAI 兼容模型的 URL、模型 ID、显示名称与 API Key，并可先测试连接再保存。
- API Key 只存入 macOS Keychain；公网 URL 强制 HTTPS，失败保存会回滚到原配置和原凭据。
- 设置和首启界面完成信息层级、中文文案、无障碍名称与按需展开优化。
- 人物继续沿用成年像素眼镜小哥、黑色连帽衫和暖橙肤色的全身形象，不走卡哇伊化路线。

## 安装

推荐使用 Homebrew：

```bash
brew tap yangwz1993-gif/haochen
brew install --cask haochen
open -a haochen
```

已安装旧版时运行：

```bash
brew update
brew upgrade --cask haochen
```

## 分发说明

0.3.2 延续上一版 legacy Homebrew 分发模式：App 使用稳定的发布者自签身份 `haochen Local Signing`，没有经过 Apple Developer ID 签名或 Apple 公证。Homebrew Cask 安装后会移除 App 的 quarantine 属性；直接下载并双击 DMG 可能被 Gatekeeper 拦截。

这不是 Apple 认证发行。希望正常安装的用户请使用上面的 Homebrew 命令。

## 验证

- 自动化检查：367 项测试通过，整体覆盖率 83%；分层视觉与集成回归 6/6 通过。
- 独立高审美用户黑盒验收：第 11 轮 **PASS**，P0/P1/P2 均为 0。
- 生命周期实测：非 hover 总停留 20.382 秒；新结果立即 hover 22.011 秒后，移开仍获得 20.339 秒完整预算；无 hover 结果 20.383 秒自然退场。
- 最终 DMG SHA-256：`90baf6617f12d7ac180845f2414ffea918a1ec977e1226a8c08f0ed1bfea693a`。

## 回滚

如升级后遇到阻断问题，可卸载 0.3.2 并从历史 Release 安装 0.3.1；不要删除 `~/Library/Application Support/haochen`，即可保留现有配置与会话数据。
