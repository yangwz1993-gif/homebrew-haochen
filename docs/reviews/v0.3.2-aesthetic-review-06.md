# haochen v0.3.2 最终独立黑盒验收，第 6 轮

## 唯一结论：NOT PASS

放行要求为 P0–P2 全部为零。本轮有 2 个 P1：结果 hover 仍被计入约 20 秒生命周期；首启自定义模型虽然验证、保存且成为设置页默认模型，但试问会话仍沿用 DeepSeek，必须手动新建会话才真正切换。

## 候选与隔离

- 唯一候选：`packaging/dist/haochen-0.3.2.dmg`。
- 实测 SHA-256：`46a6900886b21f4dbbd851045fdfbb713fd92ca2406d9d5c3d97dc6126291af9`，与指定值完全一致。
- 从只读挂载 `/private/tmp/haochen-v032-r06-mount.1Y5NGK/haochen.app` 运行，版本 0.3.2（build 2），未覆盖 `/Applications`。
- 全新 Home：`/private/tmp/haochen-v032-r06-home.WNQzWv`；全新 Keychain service：`com.haochen.qa.v032.r06.WNQzWv`。
- 正常启动显式 unset `HAOCHEN_SKIP_ONBOARDING`、`HAOCHEN_SKIP_PERMISSION_GUIDE`、`HAOCHEN_MOCK`。
- PID、父子关系、隔离 session-dir 与脱敏 Keychain 证明见 [10-environment-proof.txt](assets/v0.3.2-aesthetic-review-06/10-environment-proof.txt)。

## P0–P3

### P0

无。

### P1-1：结果文字区 hover 仍未暂停生命周期

真实本地 OpenAI 兼容模型返回 `Round 06 成功结果。` 后，以单调时钟和 100 ms 轮询执行 A 组：

1. 结果出现后指针在外运行 9,185.716 ms（目标约 8 秒；多出的约 1.2 秒为截图与跨应用切换开销）。
2. 将真实指针置于结果文字区，连续 hover 8,001.498 ms；hover 结束时结果仍可见。
3. 指针跨应用移至 TextEdit 后，结果仅 1,998.650 ms 即退场。
4. 从结果出现到退场的总墙钟为 20,101.780 ms。

若 hover 正确暂停，离开时应仍余约 11 秒，总墙钟应约 28 秒。实际仍是约 20 秒总寿命，说明 hover 的 8.001 秒全部被计入倒计时。

证据：[03-a-result-start.jpg](assets/v0.3.2-aesthetic-review-06/03-a-result-start.jpg)、[04-a-before-hover-8s.jpg](assets/v0.3.2-aesthetic-review-06/04-a-before-hover-8s.jpg)、[05-a-hover-held-8s.jpg](assets/v0.3.2-aesthetic-review-06/05-a-hover-held-8s.jpg)、[07-a-dismissed.jpg](assets/v0.3.2-aesthetic-review-06/07-a-dismissed.jpg)、[09-timing-proof.txt](assets/v0.3.2-aesthetic-review-06/09-timing-proof.txt)。其中 `05` 可直接看到真实指针位于结果文字区。

### P1-2：首启自定义模型未同步到试问会话

- 在全新隔离 Home/Keychain 中，自定义模型经 UI 验证成功并保存；设置页显示默认供应商 `127.0.0.1`、默认模型 `QA Local Round 06`。
- 首启试问以及随后短气泡提问却继续走旧 DeepSeek 会话，用户只看到：`⚠ API Key 无效或格式不正确。请打开设置重新配置后再试。`
- 运行态证据显示请求尚未到达本地夹具，旧会话模型仍是 `deepseek-v4-flash-vision-exp`；这不是假 Key 格式造成。进入完整会话并点“新会话”后，界面明确显示 `QA Local R06 / qa-local-r06`，相同自定义 Key 与夹具立即 POST 200 并成功返回结果。
- 普通首启用户不应在已验证并保存自定义模型后，还要自行理解“旧会话模型未切换”并手动新建会话才能完成试问。

证据：[01-custom-validated.jpg](assets/v0.3.2-aesthetic-review-06/01-custom-validated.jpg)、[02-full-chat-custom-success.jpg](assets/v0.3.2-aesthetic-review-06/02-full-chat-custom-success.jpg)。

### P2

无。

### P3

无。

## 其他抽查与继承证据

- 输入短气泡边界、关闭/展开/发送图标未见明显视觉回归：[00-input-before-a.jpg](assets/v0.3.2-aesthetic-review-06/00-input-before-a.jpg)。
- 成功结果右上关闭按钮、继续问与查看详情入口均存在，布局清楚；完整会话可正常打开。由于 A 已形成确定 P1，未再新增独立关闭/详情→Esc 回归样本。
- 成年像素人物保持完整全身、暖橙肤色，无灰紫、半透明或卡哇伊化回归：[08-idle-fullbody.jpg](assets/v0.3.2-aesthetic-review-06/08-idle-fullbody.jpg)。
- 首启 36 秒门禁与续办/水合引用 Round 05 已通过证据；本轮首启流程外观未见发现提示覆盖回归，但出现了上述“自定义模型未同步到试问会话”新缺陷。

## 覆盖限制

- B 组“立即 hover 至少 22 秒、移出后约 20 秒退场”未执行：A 组已经以真实结果和真实指针形成确定失败，验收协调方要求 A 失败后立即报告并停止重复。
- 正式包不含开发 mock 属预期，本轮结果来自真实本地 OpenAI 兼容模型。

## 最终结论

**NOT PASS**。A 组证实 hover 仍被计入约 20 秒结果生命周期；同时首启自定义模型未应用到试问会话，需手动新建会话才能使用。P0=0、P1=2、P2=0、P3=0，不满足 P0–P2 全零的放行门槛。

## 清理

- 已正常 TERM main PID 20868；main 与 engine PID 23243 均退出。
- 本地模型夹具 `127.0.0.1:18770` 与截图接收夹具 `127.0.0.1:8773` 均停止，端口无监听。
- 测试 Keychain 项已删除，复查返回 not found（exit 44）。
- 只读 DMG 设备 `disk8` 已卸载；隔离 Home 与证据保留。
