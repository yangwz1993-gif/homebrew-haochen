# haochen v0.3.2 独立高审美用户验收 · Round 10 Fresh

## 唯一结论

**NOT PASS**

严重度计数：`P0 = 0 / P1 = 1 / P2 = 0`。门禁要求 P0–P2 全为 0，本轮不能通过。

## 候选与有效隔离

- 唯一候选：`packaging/dist/haochen-0.3.2.dmg`
- SHA-256：`08437178386fa42506e7241c55addbd76a1d1f8fa2ef3b18b7be3389e0edc445`，严格匹配指定值。
- DMG：以 `read-only, nobrowse` 挂载，从卷内 `haochen.app` 原位启动；未复制或修改 App。
- 有效运行保留真实 `HOME=/Users/yangwz`，数据隔离到 `HAOCHEN_HOME=/private/tmp/haochen-aesthetic-r10-fresh-valid/data`。
- Keychain 使用唯一 service `com.haochen.qa.v032.r10.fresh.Z7kP2m`；结束时删除该 service 的唯一测试项。
- `HAOCHEN_SKIP_ONBOARDING`、`HAOCHEN_SKIP_PERMISSION_GUIDE`、`HAOCHEN_MOCK` 均显式 unset。
- 模型服务为真实本地 OpenAI 兼容 HTTP 夹具 `http://127.0.0.1:18432/v1`，不是 `HAOCHEN_MOCK`；请求证据见 [`fixture-valid.jsonl`](assets/v0.3.2-aesthetic-review-10-fresh/fixture-valid.jsonl)。环境摘要见 [`environment.txt`](assets/v0.3.2-aesthetic-review-10-fresh/environment.txt)。

## 一票否决问题

### P1-01 新一轮短结果沿用上一轮的剩余退场时长

- 证据编号：`H-A`、`H-B`。
- 状态与操作：
  1. A 轮结果出现后，把真实系统光标置于气泡外 8 秒；再用 `Quartz.CGWarpMouseCursorPosition` 移入结果卡并保持 8 秒；移出。
  2. A 轮退场后发起全新的 B 轮；结果出现后 1.28ms 内把真实系统光标移入，保持 22 秒；移出。
- 实际：
  - A 轮工作正确：光标外 `8.009s`，卡内保持 `8.012s`，移出后 `12.269s` 退场；总非 hover 时间约 `20.278s`。
  - B 轮在卡内保持 `22.014s`，结果确实没有消失；但移出后仅 `12.155s` 即退场，而不是新一轮应有的约 20 秒。
  - B 轮退场时长与 A 轮离开时的剩余约 12 秒高度一致，表现为新结果继承上一轮剩余倒计时。
- 测量：
  - A 轮结果窗：owner `haochen`、PID `68838`、bounds `x=1027 / y=516 / w=380 / h=193`。`appeared=259083.885332`，`entered=259091.897084`，`left=259099.912580`，`gone=259112.181263`。
  - B 轮结果窗：owner `haochen`、PID `68838`、bounds `x=1027 / y=518 / w=380 / h=193`。`appeared=259185.127017`，`entered=259185.128297`，`held_22s=259207.142171`，`left=259207.145476`，`gone=259219.300417`。
  - A/B 每个事件同时记录 `CGEventGetLocation`；A 轮外部坐标 `(900,100)` 为 `inside=false`，内部坐标 `(1217,602)` 为 `inside=true`；B 轮内部 `(1217,604)` 为 `inside=true`，移出 `(900,100)` 为 `inside=false`。
  - 原始记录：[`hover-A-pass.json`](assets/v0.3.2-aesthetic-review-10-fresh/hover-A-pass.json)、[`hover-B.json`](assets/v0.3.2-aesthetic-review-10-fresh/hover-B.json)。探针源码：[`quartz_hover_probe.py`](assets/v0.3.2-aesthetic-review-10-fresh/quartz_hover_probe.py)。
- 预期：每个新结果都应拥有独立的约 20 秒默认退场预算。B 轮出现即 hover 时，hover 期间保持；移出后应再显示约 20 秒。
- 影响：用户在第二个及后续回答上会无端少约 8 秒阅读时间；结果卡的“不打扰”节奏变得不可预测，直接降低对连续问答可靠性的信任。
- 严重度 / 置信度：`P1 / 高`。
- 复验点：连续产生两条短结果；第一条先在外部消耗 8 秒再 hover，第二条出现即 hover 22 秒；第二条移出后必须独立保留约 20 秒，不能复用第一条余额。

## 已完成的快速实际回归

| 场景 | 结果 | 实际证据 |
|---|---|---|
| 首启自定义 URL / 模型 ID / 显示名称 / Key | PASS | URL `http://127.0.0.1:18432/v1`、ID `haochen-r10-local`、名称 `R10 本地暖橙模型`，界面显示“验证成功，已保存到 Keychain”；见 [`02-custom-model-validated.png`](assets/v0.3.2-aesthetic-review-10-fresh/02-custom-model-validated.png)。 |
| 首启中断后续办水合 | PASS | 重启后回到连接模型页，provider、URL、ID、显示名称完整水合，状态为“已配置，可继续”；测试 Key 未以明文回填。 |
| 首启试问命中新模型 | PASS | 夹具收到真实 `POST /v1/chat/completions`，请求 model 为 `haochen-r10-local`；结果明确显示“已命中本地自定义模型 haochen-r10-local”；见 [`03-first-trial-result.png`](assets/v0.3.2-aesthetic-review-10-fresh/03-first-trial-result.png)。 |
| 输入气泡边界与图标 | PASS（呈现与发送） | 输入气泡 `x=1063 / y=533 / w=344 / h=176`，人物 `x=1317 / y=707 / w=112 / h=112`；关闭、展开、发送三个图标均显示且有明确可访问名称，发送按钮实际完成提问；见 [`05-clean-input-bubble.png`](assets/v0.3.2-aesthetic-review-10-fresh/05-clean-input-bubble.png)。 |
| 结果状态 | PASS（呈现） | 结果卡文字清楚、操作层级明确，A/B 均保持稳定 bounds，无跳位；但 B 轮退场预算失败，见 P1-01。 |
| 成年暖橙全身人物 | PASS | 右下角为成年像素人物完整坐姿，脸部、手部暖橙，黑色衣裤与眼镜轮廓清楚；与气泡尾巴方向关系可辨。 |

首启图见 [`01-first-launch.png`](assets/v0.3.2-aesthetic-review-10-fresh/01-first-launch.png)，输入态联合截图见 [`05-clean-input-bubble.png`](assets/v0.3.2-aesthetic-review-10-fresh/05-clean-input-bubble.png)。未发现文字裁切、控件重叠、图标失真或人物素材缺失。

## 未继续扩展的场景

发现高置信度 P1 后按硬门禁立即停止扩展测试并进入清理，因此本轮没有把“结果关闭按钮”“详情 → Esc”“输入态关闭/展开按钮”逐个再次点击到底；它们的呈现、可访问名称与边界已检查，但不能把未执行动作写成通过。这不影响本轮唯一 NOT PASS 结论。

## 无效前序运行说明（不计产品缺陷）

最初误把进程 `HOME` 指向 `/tmp`，导致 macOS 无法按正常用户环境解析登录 Keychain，并出现系统“恢复为默认”提示。该次运行立即判为 INVALID：恢复了原默认 Keychain 和 search list，终止该次 App/engine/fixture，删除临时 Keychain 并卸载其 DMG；相关弹窗不计入产品缺陷。随后才使用本报告所列真实 HOME + 独立 `HAOCHEN_HOME` + 唯一 Keychain service 完整重启并形成全部有效证据。

## 清理

本轮结束时已执行：终止候选 App 与其 engine、停止本地夹具、删除唯一 Keychain service 测试项、卸载本轮只读 DMG、清除 `/private/tmp/haochen-aesthetic-r10-fresh-valid` 隔离数据；未修改产品源码与素材。清理后的只读核验结果见 [`cleanup.txt`](assets/v0.3.2-aesthetic-review-10-fresh/cleanup.txt)。

**最终：NOT PASS。**
