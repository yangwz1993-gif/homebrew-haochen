# haochen v0.3.2 独立高审美用户验收 · Round 11

## 唯一结论

**PASS**

严重度计数：`P0 = 0 / P1 = 0 / P2 = 0`。

## 候选与隔离有效性

- 唯一候选：`packaging/dist/haochen-0.3.2.dmg`
- SHA-256：`90baf6617f12d7ac180845f2414ffea918a1ec977e1226a8c08f0ed1bfea693a`，严格匹配指定值。
- DMG 以 `read-only, nobrowse` 挂载，App 从只读卷原位启动；未修改产品代码、配置模板或素材。
- 保留真实 `HOME=/Users/yangwz`；数据仅写入 `HAOCHEN_HOME=/private/tmp/haochen-aesthetic-r11-valid/data`。
- Keychain 使用本轮唯一 service `com.haochen.qa.v032.r11.fresh.P4mN8x`。
- `HAOCHEN_SKIP_ONBOARDING`、`HAOCHEN_SKIP_PERMISSION_GUIDE`、`HAOCHEN_MOCK` 与大小写 proxy 环境均显式 unset；仅设本地 `NO_PROXY=127.0.0.1,localhost`。
- 新夹具监听 `127.0.0.1:18433`，真实处理 `/v1/models` 与流式 `/v1/chat/completions`。环境见 [`environment.txt`](assets/v0.3.2-aesthetic-review-11/environment.txt)，请求见 [`fixture.jsonl`](assets/v0.3.2-aesthetic-review-11/fixture.jsonl)。
- 本轮无 harness 异常或 INVALID 样本混入结论。

## 连续结果生命周期核心门禁

所有光标动作均由 `app/.venv/bin/python` 调用 `Quartz.CGWarpMouseCursorPosition` 完成；`CGEventGetLocation` 记录实际系统光标位置，`CGWindowListCopyWindowInfo` 只选择当前候选 PID `79266` 的结果窗口。未用 CUA hover 代替。

### A：外部 8 秒 → 内部 hover 8 秒 → 剩余约 12 秒

- `appeared = 261137.988111`，光标 `(900,100)`，`inside=false`。
- `outside_8s = 261145.999223`：外部保持 `8.011s`。
- `entered = 261146.004140`：光标 `(1217,602)`，结果窗 bounds `x=1027 / y=516 / w=380 / h=193`，`inside=true`。
- `held_8s = 261154.008872`：内部保持 `8.005s`，窗口仍存在且 bounds 不变。
- `left = 261154.018910`：光标回到 `(900,100)`，`inside=false`。
- `gone = 261166.389568`：移出后 `12.371s` 退场。
- 非 hover 总时长：`8.011 + 12.371 = 20.382s`。
- 判定：**PASS**。原始记录见 [`hover-A.json`](assets/v0.3.2-aesthetic-review-11/hover-A.json)。

### B：全新结果出现即 hover 22 秒 → 移出后重新获得约 20 秒

- `appeared = 261241.548868`，当前候选窗口 number `19139`、owner PID `79266`。
- `entered = 261241.554215`：仅 `0.005s` 后进入；光标 `(1261,660)`，`inside=true`。
- `held_22s = 261263.565312`：内部保持 `22.011s`，结果仍存在；稳定 bounds `x=1027 / y=518 / w=380 / h=193`，同一光标位置仍为 `inside=true`。
- `left = 261263.568434`：光标移到 `(900,100)`，`inside=false`。
- `gone = 261283.906960`：移出后 `20.339s` 退场。
- 判定：**PASS**。第二条结果不再继承 A 的约 12 秒余额，明确重新获得完整约 20 秒默认预算。原始记录见 [`hover-B.json`](assets/v0.3.2-aesthetic-review-11/hover-B.json)。

### C：第三条无 hover 自然退场

- `appeared = 261338.766712`，光标 `(900,100)`，`inside=false`。
- 全程不 hover；`gone = 261359.149236`。
- 自然可见时长：`20.383s`。
- 判定：**PASS**。第三条仍获得独立完整预算，未受 A/B 状态污染。原始记录见 [`no-hover-C.json`](assets/v0.3.2-aesthetic-review-11/no-hover-C.json)。

状态进入 `PRESENTING` 时窗口从动画起始 bounds `292×91` 平滑展开，计时过程的稳定结果 bounds 为 `380×193`；三轮均未见跳锚、闪烁、意外收起或 owner/PID 串线。探针见 [`quartz_hover_probe.py`](assets/v0.3.2-aesthetic-review-11/quartz_hover_probe.py)。

## 回归抽查

| 场景 | 结果 | 实际 |
|---|---|---|
| 首启自定义模型 | PASS | 在 UI 填入 `http://127.0.0.1:18433/v1`、模型 `haochen-r10-local`、名称 `R11 本地暖橙模型` 与本地测试 Key；界面显示“验证成功，已保存到 Keychain”。见 [`01-custom-model-validated.png`](assets/v0.3.2-aesthetic-review-11/01-custom-model-validated.png)。 |
| 续办水合 | PASS | 验证后终止并重新启动候选；仍回到模型页，provider、URL、ID、名称完整水合，状态为“已配置，可继续”，Key 未明文回填。 |
| 首问命中新模型 | PASS | 真实夹具收到 model=`haochen-r10-local` 的流式 POST；短结果直接显示“已命中本地自定义模型 haochen-r10-local”。 |
| 结果关闭 | PASS | 实际点击短结果右上关闭按钮，气泡收起，回到仅人物待机态。 |
| 输入展开保留草稿 | PASS | 气泡输入“草稿保留检查：不要发送”后点击展开；完整会话输入框保留相同草稿；关闭完整会话再打开气泡，草稿仍完整存在。 |
| 详情 → Esc | PASS | 从短结果点击“查看当前会话详情”，完整会话正常展示历史与两条结果；按 Esc 后立即收起并回到桌宠。见 [`03-detail-view.png`](assets/v0.3.2-aesthetic-review-11/03-detail-view.png)。 |
| 成年暖橙全身人物 | PASS | 成年像素人物为完整坐姿，脸与双手暖橙，黑色衣裤和眼镜轮廓清楚；没有素材缺失、裁切或比例异常。待机收尾见 [`04-after-C-gone.png`](assets/v0.3.2-aesthetic-review-11/04-after-C-gone.png)。 |

## 审美结论

本轮可见状态下，人物、气泡、尾巴、文字层级与按钮图标保持一致；短结果连续出现时没有残影、跳位或边界裁切。详情窗口信息层级清楚，草稿与发送区未被历史内容挤压。未发现可归入 P0–P2 的新问题。

## 清理

完成后已终止本轮 App、engine 与夹具，确认端口 `18433` 无监听；已删除唯一 Keychain service 测试项、卸载只读 DMG，并移除 `/private/tmp/haochen-aesthetic-r11-valid` 隔离目录。清理核验见 [`cleanup.txt`](assets/v0.3.2-aesthetic-review-11/cleanup.txt)。

**最终：PASS。**
