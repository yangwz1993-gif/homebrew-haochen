# haochen macOS App · 交付归档索引（P0→P7 可交付上市）

> 日期：2026-08-25 ｜ 产品判定：**可交付上市**（.dev-loop-done 已置位）
> 本文件是完整交付的归档索引。各阶段细节见对应文档。

---

## 一、交付产物（packaging/dist/）

| 产物 | 路径 | 说明 |
|---|---|---|
| 安装镜像 | `packaging/dist/haochen-0.1.0.dmg`（~71MB） | UDZO，含 Applications 拖装链接 + 像素小哥卷图标/App 图标 |
| App 包 | `packaging/dist/haochen.app`（~150MB） | PyInstaller onedir；LSUIElement（Dock 无图标）；内嵌引擎 + 读屏执行体 + 扩展 + 配置模板；ad-hoc 签名（外发需 Developer ID+公证） |
| 一键构建 | `packaging/build.sh` | 读屏执行体→图标→PyInstaller→资源内嵌→签名→dmg，可重复 |
| 签名设置 | `packaging/setup-signing.sh` | 自签受信任证书（选配，授权跨构建持久；需一次管理员密码） |
| 全量回归 | `tests/run_all_regressions.sh` | L1–L4 一键跑（`--real` 含真引擎） |

## 二、里程碑交付文档（docs/）

| 阶段 | 文档 | 结果 |
|---|---|---|
| P0–P2 引擎/契约/模型层 | 开发总纲 §二 | ✅（前期），契约 `rpc-contract.md` v1.1 冻结 |
| P3 三 UI | `p3-delivery-mc-me.md` | M-C 对话 5/5、M-E 桌宠 17/17、M-D 26/26、L1 45/45 |
| P4 集成 | `p4-delivery-integration.md` | 单壳+引擎进程管理+三 UI+双入口同会话+崩溃恢复；真链路 16/16×2；修 mock stdio 吞行 bug |
| P5 打包 | `p5-delivery-packaging.md` | dmg/app；冻结 App 真机 UI 驱动问答；权限引导；内嵌读屏执行体 |
| P6 回归打磨 | `p6-delivery-regression.md` / `p6-review-fix.md` | L1–L4 全量；长时稳定 30 轮无卡死/RSS 无泄漏；多模型切换；图标；动效对齐；打回 3 项修复 |
| P7 视觉增强 | `p7-delivery-vision.md` / `p7-vision-plan.md` | 读屏截图+原图优先喂图+尺寸优先级+屏幕录制引导；pi 图像路由验证通 |
| 测试/QA | `qa-script.md`（21 步用户视角操作单）、`visual-spec.md`、`interaction-spec.md` | — |
| 签名 | `signing-guide.md` | 自签证书/Developer ID 说明 |

## 三、能力清单（对照 product-acceptance DoD 6 条）

1. **对话可用** ✅ 流式/Markdown/工具卡/会话管理/历史持久化
2. **桌宠可用** ✅ 像素小哥常驻/双击⌃⌥P唤起/读屏确认/两步回答/气泡短结+面板详答
3. **配置可用** ✅ 模型/Key/provider/思考档有 UI，改后生效/重启提示，持久化
4. **引擎独立** ✅ Bun 单文件引擎，不依赖全局 pi，数据在 ~/Library/Application Support/haochen/
5. **分发可用** ✅ ad-hoc .dmg 本机可装（外发需 Developer ID+公证）
6. **常驻无 Dock** ✅ LSUIElement=true

**多模态视觉**（追加）：读屏截图 + 原图（URL 下载，面积优先）喂 flash-vision，识人/看图界面；双权限引导（辅助功能+屏幕录制）。

## 四、验证证据（最终）

- 全量回归 `tests/run_all_regressions.sh --real` → **PASS=7 FAIL=0**（L1 45/45，L2 26/26，M-C 5/5，M-E 17/17，P4 25/25，真链路 16/16，L4 打包）
- 稳定性：30 轮无卡死、会话增长 120 条不丢、RSS 110→114MB 无泄漏、50 会话堆积、3 次崩溃自动恢复
- 多模型切换：deepseek flash/pro/flash 真答 + 同会话 + 配置持久化
- 图标：像素眼镜小哥（CFBundleIconFile + 卷图标）非默认
- 收口标记：`.dev-loop-done`

## 五、遗留提示（非阻塞）

- 外发/他人安装：需 **Developer ID 签名 + 公证**（总纲 §五 既定）；本机/内测 ad-hoc 可用。
- ad-hoc 签名下授权（辅助功能/屏幕录制）不跨构建持久；用 `setup-signing.sh`（自签受信任证书）或 Developer ID 则持久。
- 精细动效（弹性/贴边露头等）按 visual-spec §5 属「本期不强求」，可后续版本打磨。
