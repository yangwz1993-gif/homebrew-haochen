# Changelog

本项目从 `0.2.0-dev.1` 起遵循 [Semantic Versioning](https://semver.org/)；正式发布记录按 Keep a Changelog 维护。

## [Unreleased]

## [0.3.4-rc.6] - 2026-09-11

本机窗口感知与阅读目标验收候选，未更新线上 Homebrew 或 Release。

### Changed
- 每轮提问绑定应用、窗口及页面标识；阅读确认显示目标，不在切屏时主动询问。
- 新问题不再默认沿用历史屏幕结果，明确继续历史对象时保留上下文。
- 读取前后核对固定窗口及内容，拒绝窗口关闭、页面变化和目标不明；排队等待不替换目标。
- 真实 AX 文本和原图优先，截图提前采集仅作辅助；保留标题/链接结构、标注缺失原图和读取范围。
- 排除 macOS 屏幕共享微型浮层，取消不必要的读前辅助进程和默认自动滚动。

## [0.3.4-rc.5] - 2026-09-11

本机配置生效流程验收候选，未更新线上 Homebrew 或 Release。

### Fixed
- 修复首次填写 Key 已验证保存、DeepSeek 聊天引擎却仍持有旧环境而无法使用的问题。
- 初始化、新 Key、已有 Key、自定义服务及模型切换共用配置生效流程：等待当前回合结束，按需重新加载凭据，恢复会话，等待模型选择应答，再核对运行中的模型。
- 不再让用户手动确认“重启引擎”；模型尚未就绪时不显示已连接，不能发送新请求。失败保留配置并可重试，不误报为 Key 无效。
- 首启向导只有确认模型就绪后才标记完成、发送试问；取消过程中不再自动发送。

### Tests
- 使用真实引擎与本机 HTTP 服务，验证先启动无 Key 引擎，再通过初始化填写 Key，首次回答成功；换 Key 后请求使用新凭据且保留已有会话。覆盖预设及自定义两条路径。

## [0.3.4-rc.4] - 2026-09-11

本机连接流程验收候选，等待所有者验收；未更新线上 Homebrew 或 Release。

### Fixed
- 首启和设置对每个连接入口只保留“连接模型”主按钮；新 Key 验证后保存，已有 Key 按需取得系统许可后继续验证，不再停在“已授权、连接待确认”。
- 连接或保存失败可用同一按钮重试；取消授权不继续验证，不覆盖旧 Key。后台旧 Key 的验证结果不会把新输入误标为已连接。
- 权限页分别显示“管理权限 / 去开启 / 重新检查”，说明重装可能保留系统授权；检查异常不会误报已授权。
- 自定义服务地址变化时要求新 Key，避免旧 Key 被发往新地址；原服务重连保留 Responses / Completions 协议。

### Changed
- 首启和设置复用会话的中性白灰、墨绿及细描边，缩小首启默认窗口。
- 更换、删除 Key 收入更多菜单；自定义模型编辑展开时收起已添加列表，避免同一模型出现重复连接入口。

## [0.3.4-rc.3] - 2026-09-11

本机视觉与交互验收候选，等待所有者验收；未更新线上 Homebrew 或 Release。

### Fixed
- 短气泡、详情及完整会话共用中性白底/深灰/墨绿样式，去掉详情页残留的黄色底与厚重框线。
- 详情展开不再重新定位人物；按当前屏幕的可用空间选择上方或侧边，空间不足时缩小面板。
- 展开/收起改为固定尺寸短距离淡入淡出，避免文字在窗口拉伸过程中反复换行。
- 两个输入入口同步未发送草稿，同一回合再次展开恢复阅读位置。

### Changed
- 合并会话标题与原生窗口栏，保留 macOS 红黄绿按钮和窗口拖动；长标题省略、首轮引导标题弱化。
- 收起使用小型图标按钮，保留 Esc / ⌘W；消息标签、按钮和卡片使用更轻的层级。

## [0.3.4-rc.2] - 2026-09-10

本机修订候选，替换 rc.1；未更新线上 Homebrew 或 Release。

### Fixed
- 用户主动“保存并验证”或删除 Key 时恢复 macOS 正常授权能力；静默启动、状态检查和后台刷新仍禁止弹出授权框。
- 旧版 Key 因访问控制无法静默读取时，保存新 Key 不再在读取旧 Key 阶段失败，也不会因回滚误删旧 Key。
- 保存失败提示改为下一次显式保存时确认 macOS 授权，不再要求用户手动“解锁钥匙串”。

## [0.3.4-rc.1] - 2026-09-10

本机一期验收候选，等待所有者验收；未更新线上 Homebrew 或 Release。

### Fixed
- 空输入再次保存不再删除现有 API Key；删除凭据改为独立确认操作。
- 连接验证与凭据保存使用后台任务，界面显示进度；后台连接状态更新不再重建正在编辑的表单。
- 首启模型表单在验证和保存期间锁定，保存成功明确显示已连接；自定义服务需返回有效列表并完成模型试答。
- 首启引导不再遮挡系统授权窗口；已拒绝的权限可以进入对应系统设置页。
- 补齐自签名升级后的“授权已有 Key”入口：仅点击时请求系统授权，支持取消重试；单次许可只缓存在当前进程，不修改或放宽钥匙串 ACL。
- 两种展开入口均可关闭和返回人物，并按当前问题定位会话；修复布局变化覆盖滚动锚点的问题。
- 读屏截图按文字来源窗口的几何和标题匹配，避免误选最大窗口或其他应用。
- 授权确认区消失后立即重算气泡高度，简答清理外露的等号高亮标记，图标适配 Retina。

### Changed
- 短气泡统一中性底色，取消内层卡片和空标题栏；普通等待使用紧凑思考点动效，并保留停止入口。
- 首启及设置称谓统一为“桌面助手”；人物风格、肤色与现有用户数据保持不变。

## [0.3.3] - 2026-09-10

### Fixed
- 修复从 0.3.1 ad-hoc 签名升级后，启动读取旧 Keychain 条目会弹出无法关闭的系统授权框；启动与状态检查改为无交互读取，并迁移到稳定签名专用的 Keychain 服务空间。
- 升级引导现在会说明重填 Key 是为避免旧版钥匙串弹窗；自定义模型用户的 URL、模型 ID 和显示名保持不变，只需重填 Key。

## [0.3.2] - 2026-09-10

### Added
- 短气泡新增右上角关闭按钮，以及与发送并列的完整会话展开图标；展开时保留尚未发送的草稿。
- 首启向导新增用户称呼确认步骤；设置页可随时修改或清除称呼。
- 首启与设置均支持安全添加、测试、编辑和删除 OpenAI 兼容模型的 URL、模型 ID、显示名称和 API Key。

### Changed
- 结果气泡默认停留 20 秒，鼠标位于气泡内时暂停计时，离开后继续剩余时间。
- 自定义模型表单在设置页按需展开，减少默认页面密度；首启向导统一为产品视觉并完成中文按钮文案。
- 桌宠唯一名称固定为小写英文 `haochen`，没有中文名；未确认用户称呼时只自然称“你”。

### Fixed
- 旧版 `{"name":"阿晨"}` 档案不再被模型静默当作用户称呼；仅在界面中作为待确认迁移候选显示。
- 自定义模型切换不会复用 DeepSeek 的验证状态；连接失败或保存异常时原模型配置和 Keychain 凭据保持不变。
- 自定义公网模型地址强制 HTTPS，拒绝 URL 内嵌凭据、查询密钥和未知模型 ID，认证内容不写入配置文件。
- 修复测试隔离中凭据写入故障桩泄漏到后续用例的问题。
- 修复 macOS 跨应用悬停时短结果倒计时无法可靠暂停或恢复的问题；现使用 Quartz 系统光标与本进程窗口几何进行同坐标系判定。
- 修复连续结果继承上一条剩余停留时间的问题；每条新结果都会获得独立完整的 20 秒预算。

## [0.3.1] - 2026-09-09

### Changed
- 将桌宠输入态从宽重的工具条收敛为更轻量的漫画气泡：缩短整体宽度、简化占位文案、减小发送按钮，并用单条焦点线替代嵌套圆角框。
- 键盘操作说明移入无障碍描述与按钮提示，减少常驻视觉噪音，同时保留回车发送、Command+回车换行和 Esc 收起能力。
- 桌宠升级为待机、思考、警示三态全身像，延续原有成年像素角色、黑色连帽衫、反光方框眼镜与暖橙肤色。

### Fixed
- 修复输入编辑区越过气泡主体下描边、压入尾巴区域的布局错误。
- 输入区现在可随一至三行内容自动增高，第四行起在框内滚动；普通、顶部翻转以及内容与输入并存时均保留明确的上下安全留白。
- 修复首次问候与输入区同时出现时的大片空白，以及窗口销毁后延迟滚动回调偶发触发的 Qt 段错误。
- 姿态切换不再把人物淡到接近透明，避免彩色桌面透过面部造成瞬时灰紫色肤色漂移。

## [0.3.0] - 2026-09-09

### Added
- 新增单轮临时结果卡：只显示当前简答，并提供“继续问”和“查看详情”两个明确动作。
- 新增结果 8 秒自动退场以及继续提问、详情关闭不恢复旧结果的回归测试。
- 新增开发构建专用启动脚本，隔离数据目录、Keychain 服务与正式版权限指纹治理。

### Changed
- 将完整源码与 Homebrew Tap 历史合并到统一仓库，并补齐 README、CI、CODEOWNERS、Issue/PR 模板、安全政策和版本发布规范。
- 明确后续发布标签必须指向完整源码提交；根 `Casks/haochen.rb` 成为唯一 Cask 发布入口。
- 开始 `0.3.0-dev.1` 开发；结果阶段不再恢复输入框、历史流或滚动条，“展开详细”改为“查看详情”。
- 将回答协议从 `answer → summary` 两次模型请求升级为单回合 `brief + detail`；桌宠直接显示简答，详情页按结论在前、完整依据在后呈现。
- 修正 mock 简答与端到端门禁：协议词泄露、首帧空白或详情关闭后结果复现都会导致验证失败。
- 真引擎隔离验收扩展到简答解析与 Keychain 引用，普通问答、多轮上下文、读屏确认和崩溃恢复共 19 项通过。

### Fixed
- 统一最终交互概念图四个人物的肤色，并锁定现有 haochen 成年工程师形象规范。
- 修复从桌宠结果卡打开详情时被强制定位到会话底部的问题；现在每次从会话顶部开始阅读。
- 桌宠右键菜单去除“设置（占位）”文案，直接打开现有设置入口。
- 自动退场计时器在气泡窗口销毁时立即停止并解除全局事件过滤器，避免慢速 CI 中访问已删除 Qt 子控件。
- 新协议解析兼容 `brief` 错误闭合为其他结果标记；真实模型即使发生标记漂移，也优先保住原始短结而不是从详答重新截取。

## [0.2.0] - 2026-09-02

### Added
- 建立独立的新版本源码基线、Git 管理和发布规划。
- 新增 v0.2.0 开发方案与缺陷修复验收门禁。
- 建立 Python/Node/Bun 版本锁、uv/npm 锁文件安装和统一 `make check` 质量入口。
- 新增版本一致性、secret、ruff、pyright、shellcheck、pytest 与覆盖率检查。
- 以根 `VERSION` 驱动 App、引擎清单、DMG/Info.plist 与 Cask 发布模板。

### Fixed
- 修复气泡长中文文本横向裁字与异常高度（Qt 无空格 CJK 文本最小尺寸缺陷），错误提示转为人类可读文案（如“API 账户余额不足”）。
- 会话删除仅允许操作私有会话目录内的直接 `.jsonl` 文件，并拒绝路径穿越、越界绝对路径和符号链接。
- 删除当前会话前必须确认已切换到新会话；删除失败会保留并切回原会话。
- 会话删除增加二次确认、可见错误和私有回收站撤销能力。
- 引擎停止改为后台回收，UI 不再同步等待；SIGTERM 超时后强杀整个进程组，避免孤儿进程。
- RPC 请求加入 5 秒超时和崩溃/停止统一结算，坏 JSON、半行输出与 stderr 以无正文摘要写入轮转诊断日志。
- supervisor 增加 30 秒健康探针；无响应时进入可见的自动重启流程。
- 当前会话改由共享 SessionCoordinator 原子持久化，成功切换立即更新，切换失败保持原会话；重启恢复不再依赖滞后的探针状态。
- 大窗口和气泡共用一条跨入口 FIFO 输入队列；消息直到引擎确认写入会话后才出队。
- 崩溃时未确认消息以 0600 草稿保留且不自动重复发送，完整窗口提供“重新发送/取消”；坏队列文件会先备份。
- 流式渲染按 50ms 节流；仅在用户位于底部时自动跟随，回看时保持位置并提供“回到最新”。
- 两入口排队消息均可见、可单独取消；工具卡覆盖运行/成功/失败/取消状态，支持取消运行中工具、失败后重试和打开 write/edit 产物，并显示耗时。
- 新增原生菜单栏（打开对话 ⌘1 / 设置 ⌘, / 新会话 ⌘N / 关于 / 退出 ⌘Q）；对话窗口记住尺寸和位置；气泡锚定桌宠所在屏；尊重 Reduce Motion；关键控件补齐 accessible name。

### Security
- 新版本基线不包含旧工程中的签名私钥、证书、口令或本地认证数据。
- 远程图片仅允许解析到公网地址的 HTTPS/443，并在每次重定向时重新校验；拒绝本机、私网、link-local、非法 MIME 和超限响应。
- 用户提问不再写入 `last-user-text.txt`，仅以 0600 文件保存派生的看图意图 boolean，并自动删除旧版明文 sidecar。
- API Key 保存至 macOS Keychain；`auth.json` 仅保留 `$HAOCHEN_*_API_KEY` 引用，写入前先通过固定安全端点验证，失败不覆盖旧 Key。
- 首次启动改为单一可续办向导（欢迎→Key 验证→按需权限→试问），不再读取全局 `~/.pi` 凭据、不再同时弹出多个系统授权页面。
- 应用数据目录统一修复为 0700，认证、会话回收、日志与运行 sidecar 统一为 0600；引擎子进程使用 0077 umask。
- 默认 release 构建仍要求 Developer ID、hardened runtime、timestamp、公证和 staple；0.3.0 经所有者明确授权，显式使用发布者自签 legacy 构建。
- 0.3.0 Homebrew Cask 以 `postflight_steps` 移除该 App 的 quarantine；README 与 Release Notes 必须说明它未经 Apple 公证，直接下载 DMG 可能被 Gatekeeper 拦截。
- 清理旧工程遗留的明文模型凭据与 Keychain 口令文档，并加强 secret scan 规则。
