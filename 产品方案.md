# haochen 产品方案（PRD · 产品视角）

> 版本：v1.0 / 2026-08-25
> 撰写角色：产品（本文档聚焦**核心交互逻辑 / 前端展示 / 核心体验流程**，以**最大化用户体验与交互美观**为最高目标，不展开研发细节。）
> 研发评估对象：本文档交付给 kimi（研发），由其评估可行性与工作量，并参考 `~/Desktop/Skills/iterm2-agent-comm/SKILL.md` 回复。

---

## 0. 一句话定位

**haochen 是一个「长在屏幕上、能看见你屏幕、主动帮你干活」的技术专家型 AI 桌面伙伴。**

它不完全是一个"聊天窗口"，而是一个**有形象、有眼睛、有工具、会在你身边的 agent**：
- 像素眼镜小哥形象（亲切、可信、像伙伴）
- 能**看见你的屏幕**（读屏）并理解你正在做什么
- 能和你的终端/文件系统交互（跑命令、读文件）
- 回答用**技术专家**的口吻：结论先行、能查证、能跑、不装懂

---

## 1. 核心用户画像与场景

- **用户**：一个整天对着屏幕（Chrome/终端/编辑器/文档）工作的人，常需要"看一眼 → 帮我梳理/改一下 → 给我结论"这类即席辅助。
- **典型场景**：
  1. 看到某个网页/报错/长文，想让 haochen"帮我看看这是什么、怎么处理"。
  2. 电脑上有多个窗口，想让它"找到那个写了一半的文档，帮我补完"。
  3. 需要一个助手在桌面随时待命，不用切到某个 App / 终端再粘贴。
  4. 想让它动手：读屏、跑命令、查文件，然后给结论和下一步。

---

## 2. 核心体验（用户旅程）

### 旅程一：看见屏幕 → 问一句 → 得到答复（最常用）

```
[桌宠挂在屏幕角落，idle]
   │ 用户双击桌宠（或按快捷键 ⌃⌥P）
   ▼
[桌宠眨眨眼 → 唤起输入气泡（带一只"眼睛对准屏幕"的姿态）]
   │ 用户输入：比如 "帮我看看这个报错"
   ▼
[haochen 先读屏确认上下文 → 气泡顶部显示"我正看一下你的屏幕…"]  ← 感知
   │ 用户确认 / haochen 自动继续
   ▼
[haochen 思考（桌宠进入 thinking 姿态，气泡出"正在想…"）]          ← 推理
   │
   ▼
[haochen 动手：读到信息 → 必要时跑命令/查文件 → 得结论]            ← 行动（工具可见）
   ▼
[气泡流式输出结论：先一句话结论（L1 短结）+ 要点/细节（L2 详答）]   ← 收敛
   │ 用户可继续追问
   ▼
[用户点空白 / Esc 收起 → 桌宠回到 idle，结论留在会话里]
```

### 旅程二：交代一个多步任务

```
用户："帮我把这段文档整理成清晰的步骤，并输出到桌面"
   ↓
[haochen 读屏拿到文档 → 思考 → 逐步行动（读/整理/写文件），每一步在气泡里可见]
   ↓
[给出结论 + 产物路径 + 下一步建议]
```

### 旅程三：中断 / 纠偏（体验不被打断的原则）

- 任何时刻用户可**打断**（Esc / 点空白收起）。
- 任何时刻可**追问**（连续对话，上下文不丢）。
- haochen 若不确定（比如读到的屏幕不清晰、需要权限），**先问再动**，不擅自猜。

---

## 3. 形象与人设（必须保持）

### 3.1 形象：像素眼镜小哥
- 像素风/复古质感，戴眼镜，传达"技术宅 · 聪明 · 可信"。
- **三个基础姿态**：
  - `idle`：安静待着，偶尔眨眼/微动（有生命感）
  - `thinking`：手托腮 / 眼睛向上瞟（在思考）
  - `angry/alert`：皱眉（遇到错误、权限被拒、需要用户注意时）
- 姿态随状态自动切换，让用户**不靠文字也能感知 haochen 在干嘛**。

### 3.2 人设：技术专家
- **口吻**：冷静、专业、结论先行；用词精准，不绕弯。
- **行为准则**：
  - 能看见就先看（读屏），不空口猜。
  - 能查证就查证（跑命令/读文件），给可靠结果。
  - 不装懂：不知道就明说，并给出怎么查的思路。
  - 给结论 + 给理由 + 给下一步。

---

## 4. 前端展示设计

### 4.1 双入口，统一设计语言

| 入口 | 形态 | 定位 |
|------|------|------|
| **A 桌面宠物（常驻）** | 像素小哥 + 唤起的**气泡** | 即席、轻量、不离屏 |
| **B 完整应用窗口** | 对话主界面 + 配置面板 | 长对话、查看历史、管理配置 |

**统一设计语言**（贯穿 A/B，也是产品视觉核心）：
- **底色**：米白 `#fdf6e3`（暖、不刺眼、护眼）
- **描边**：深 `#2b2b22`，粗描边（像素/复古质感）
- **圆角**：中等（16px 级别），气泡右下带**小尾巴**（指向桌宠，强化对话来源）
- **字体**：清晰、等宽弱感（技术感），中英文混排良好
- **动效**：克制但"有生命"——气泡弹出、姿态切换、流式文字 & 工具过程逐步浮现

### 4.2 入口 A：桌面宠物（气泡式对话）

**布局（气泡从上到下）**：
```
┌─────────────────────────────────┐
│  [👓] haochen            ✕      │   ← 条头：形象小图标 + 名字 + 收起
├─────────────────────────────────┤
│  「我正看一下你的屏幕…」          │   ← 感知/状态（轻提示，弱化显示）
│                                  │
│  （结论 L1）这个问题是 X          │
│  （要点 L2）… 原因/步骤/产物 …    │   ← 对话流：输入→思考→结果逐个弹出
│                                  │
│  [输入框]                ➤       │   ← 快捷输入（不占满，融入气泡）
└─────────────────────────────────┘
                                    ↘ (小尾巴指向桌宠)
```

**交互要点**：
- **唤起**：双击桌宠 / 全局快捷键 `⌃⌥P`（Ctrl+Option+P）。
- **收起**：点空白 / Esc / 右上 ✕。**失焦不自动收起**（"想停下来就一直挂着"，不误伤）。
- **输入**：`⏎` 发送，`⌥⏎` 换行（和主流对话产品一致）。
- **气泡不固定为输入框**，而是**动态对话流**：输入 → 思考 → 结果，三个阶段**逐条弹出**（带轻动效，像真实对话在发生），而非一个静态输入框。
- **感知状态**：读屏时给一个"看一看"的轻提示 + 桌宠"眼睛对准屏幕"姿态，让用户知道它在看。

### 4.3 入口 B：完整应用窗口

**布局**：
```
┌────────────┬──────────────────────────────┐
│  会话侧栏    │  对话主区                      │
│  · 昨天     │  [用户] 帮我看看这个报错          │
│  · 刚聊的    │  [haochen] 我正看一下你的屏幕…    │
│  · 归档     │  [haochen] (L1) 这是依赖版本冲突   │
│  + 新会话   │           (L2) 原因… 方案… 产物…  │
│            │  [工具卡] ▸ 已读屏 ✓ ▸ 已运行 cmd ✓ │
│────────────┼──────────────────────────────┤
│  设置入口   │  [输入框]               ➤        │
└────────────┴──────────────────────────────┘
```

**要点**：
- **对话流**：用户 / haochen 消息清晰分区；haochen 的消息里工具调用以**可折叠"工具卡"**呈现（读屏/命令/文件每一步可见、可点击看详情）。
- **Markdown 渲染**：代码块、列表、表格、链接清晰。
- **流式输出**：文字逐步浮现（打字机+定时），工具卡动态插入。
- **会话管理**：左侧列表（按时间/项目分组），可新建/重命名/归档；**桌宠与窗口共享同一会话**（不割裂）。
- **配置面板**：模型/provider/API Key/主题/行为开关（详见 §6）。

---

## 5. 核心交互元规则（体验主导）

1. **感知可见**：haochen "在看" 时一定要让用户知道（轻提示 + 姿态），不默默读屏。
2. **行动可见**：每次工具调用（读屏/命令/文件）都以"工具卡"呈现，用户能看懂它在干什么，可展开、可暂停。
3. **结论先行**：先说最关键的结论，再给依据/步骤/产物。
4. **先问再动**：不确定 / 需要权限时，先确认再执行。
5. **随时可打断**：Esc / 点空白收起，不打断当前心智负担。
6. **连续性**：对话上下文不丢，能在气泡和窗口间无缝继续。
7. **不打扰**：不需要用户注意时安静待着（idle），只在"需要你确认/出结果"时以轻提示唤起。

---

## 6. 配置与个性化（轻量、不吓人）

- **模型/provider**：一键切换（默认 DeepSeek），预留多 provider。
- **API Key**：安全存储，可测试连通。
- **主题**：米白（默认）/ 暗色 / 其他，跟随系统可选。
- **行为开关**：是否读屏前确认、是否自动跑命令、音效开关、图标大小/位置。
- **人设微调**：语气强度（更简洁/更详细）。

配置入口：桌宠右键"设置" / 完整窗口侧栏"设置"。设置项**少而清晰**，默认值即好用，避免吓到用户。

---

## 7. 关键体验指标（PRD 验收参考）

- **唤起直达**：从唤起气泡到能输入，≤1s，无多余步骤。
- **感知信任**：读屏动作用户"可感知、可理解"，不产生"它在偷看"的疑虑。
- **结论清晰**：回答先结论、有依据、有下一步。
- **连续性**：气泡↔窗口会话一致，追问不丢上下文。
- **不误伤**：失焦不收起、不打扰（idle 安静）。
- **视觉统一**：A/B 入口设计语言一致，像素小哥+米白+描边贯穿。

---

## 8. 交付物（这一版产品方案对应的产品）

- 桌面宠物（像素眼镜小哥，三姿态）+ 气泡式动态对话流 + 读屏/查证/跑命令能力（延续,不去掉）。
- 完整应用窗口（对话 UI + 会话管理 + 工具卡）+ 配置面板。
- 统一设计语言 + 动效 + 人设。

### 与本机 pi「零共享」
- haochen 是独立应用，**不读/不写本机全局 pi 的配置、会话、数据**（技术侧由研发落实，此处为产品对"独立性"的约定：用户感知上它是独立的、不会被 pi 的配置/版本影响）。

---

## 9. 待研发评估的问题（交给 kimi）

1. **引擎**：用 pi 源码只拷贝逻辑、去外部依赖，做成独立 macOS App（含对话/配置/桌宠，交付 .dmg）——工程可行性与最大风险点？
2. **读屏/权限**：辅助功能+屏幕录制授权、首次运行引导，体验如何做得顺？
3. **动态对话流 + 桌宠姿态联动**：气泡逐条弹出动效、感知提示，实现成本？
4. **双入口会话共享**：气泡与完整窗口共享会话，不割裂，体验如何保证？
5. **打包**：PyQt + 内嵌引擎 → .app → .dmg 的可行性与签名/公证注意事项？

> 以上请 kimi 评估（研发视角），并参考通信总线规范回复；产品侧已按"体验优先"定义好可感知的交互与视觉目标，研发需在不牺牲上述体验目标的前提下给出可实现路径。
