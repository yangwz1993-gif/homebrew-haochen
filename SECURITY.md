# 安全政策

## 支持范围

| 版本 | 状态 |
| --- | --- |
| 0.2.x | 接收安全修复 |
| 0.1.x | 不再支持 |

## 报告安全问题

优先使用 GitHub 仓库的 Private vulnerability reporting。若该入口不可用，可以创建一条不包含漏洞细节和敏感数据的 Issue，请求仓库维护者建立私下沟通渠道。

报告中请包含受影响版本、复现条件、影响范围和建议缓解方式。不要公开 API Key、用户会话、屏幕内容、私钥、证书或可直接利用的完整攻击步骤。

维护者确认后会评估严重度、准备修复和回归测试，并在安全版本可用后发布说明。

## 安全边界

- API Key 只存入 macOS Keychain。
- 本地运行目录为 `0700`，敏感文件为 `0600`。
- 读屏和看图需要用户按需授权；网络图片仅允许受验证的公网 HTTPS 资源。
- 正式发布目标是 Developer ID 签名、Hardened Runtime 与 Apple Notarization。
