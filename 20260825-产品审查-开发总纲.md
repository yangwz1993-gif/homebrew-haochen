# 产品视角审查 · haochen macOS App 开发总纲（kimi 版）

> 审查方：haochen（产品） ｜ 审查对象：`20260825-haochen-macOSApp开发总纲.md`
> 总体印象：**总纲质量高、工程化严谨**（契约先行、文件互斥、验证才算完成、分层测试、风险登记），作为执行层唯一依据我认可。以下按你问的三点给产品判断，并指出**一处需要对齐的冲突**。

---

## 一、§一 DoD 6 条 vs 产品预期

**结论：6 条是合格的「可用」底线，我认可作为交付门槛。** 逐条对照产品方案（`产品方案.md`）：

| DoD | 产品预期 | 判定 |
|---|---|---|
| 1 对话可用（流式/MD/工具可见/会话） | M-C 核心 | ✅ 覆盖 |
| 2 桌宠可用（像素小哥/唤起/读屏确认/两步） | M-E + 形象人设/读屏 | ✅ 覆盖，形象三姿态在 |
| 3 配置可用 | M-D | ✅ 覆盖 |
| 4 引擎独立（Bun 单文件/不依赖/数据独立） | 独立目标 | ✅ 覆盖 |
| 5 分发可用 | 交付 dmg | ✅ 覆盖 |
| 6 LSUIElement 常驻无 Dock | 桌宠定位 | ✅ 好，是产品加分项 |

**建议补充（不一定要进硬 DoD，但务必进 P6 回归清单/质量标准）**：
1. **双入口会话共享一致**：气泡 ↔ 完整窗口**同一会话**、追问不丢上下文（产品方案 §4.3 明确要求）。DoD 未点明，建议加进 DoD 或作为 P4 集成验收。
2. **感知/行动可见**（产品方案 §5）：读屏动作用户可感知、"工具过程可见"已含，但"正在看屏幕"的**轻提示**建议补齐（不强求，回归项）。
3. **不误伤**：失焦不自动收起、idle 不打扰 —— 作为 P6 回归项，不是硬 DoD。
4. **视觉统一**（产品方案 §4.1）：像素小哥 + 米白 `#fdf6e3`/深描边/圆角/尾巴贯穿 A/B。DoD 未提。建议在 P1 冻结一份**视觉规范**（色值/描边/圆角/尾巴/字体/动效节奏），三 UI 共用，保证并行不跑偏。

---

## 二、§二 路线图 P0–P6 优先级

**结论：优先级合理，关键路径清晰（P0→P1契约→P3 M-C→P4→P5→P6），我认可。** 产品侧两点小意见：
- **P1 契约先行**：非常对，避免 P3 三 UI 返工，是产品最在意的「稳定不返工」。
- **P3 中 M-C（对话）是关键路径瓶颈（5–8d）**：产品层面最认可 M-C 排最大头——对话是用户最常接触的核心界面。建议 P3 并行时把 M-C 当作「准关键路径」盯紧。

---

## 三、§三 P3 三 UI 并行是否认可

**结论：认可并行（提效、符合工程实践），但有两个前置要求**，否则并行会牺牲产品强调的「美观 + 体验」：
1. **并行前冻结「视觉规范」**（我上面 D4 提到）：色值、描边、圆角、尾巴尺寸、姿态图、字体、动效节奏。三个 agent 各做各的，**没有统一规范必然跑偏**，视觉统一是产品明确目标（§4.1）。
2. **并行前冻结「交互规范」**（产品方案 §5 元规则）：感知可见、行动可见、结论先行、先问再动、随时可打断、不打扰。让三 agent 对 mock 开发时也统一交互手感。

> 这两份规范**产品侧（我）可以现在就提供**，作为 P1 的一部分或 P3 前置，不强求技术，是设计约定。

---

## 四、⚠️ 需要对齐的冲突点（重要）

总纲「§三·3 最小改动，不发明需求」里写：**「美化、贴边露头、动效等均不在本期范围。」**

**但「动态对话流 + 基本动效」是产品的明确核心诉求，不是「美化」，而是「交互形态」**（产品方案 §4.2、§7 关键体验指标）：
- 用户明确要求气泡**不是固定输入框，而是「输入→思考→结果」逐条弹出、带动效**（动态对话流）。
- 产品方案把「感知提示」和「姿态切换动效」列为提升「有生命感」的**关键体验**。

**所以我建议**：不要把「动效」整体排除在 P3 之外。至少保留 **MVP 级动效**（气泡逐条弹出、桌宠姿态切换、流式打字机），把「精细动效（弹性/缓动、贴边露头动画）」放 P6 或后续。**「动态对话流」必须在 P3 对话前端实现**，否则产品最核心的交互诉求被砍。

请你确认这一点（这是总纲与我产品方案唯一的实质性冲突）。

---

## 五、补充（产品侧认可、无需改）

- QA 人肉模拟（§四.3 M0 复用）：非常好，产品信赖，保证体验不走样。
- 隔离原则（§三.6）：符合「与本机 pi 零共享」，✓。
- 回归清单（§四.4）覆盖了产品关注项；建议把上面 D1-D4 并入回归清单。

---

**总结**：总纲整体我认可，可作为执行依据。唯一需对齐/修改的是 **§三.3 的「动效不在本期」** —— 请改为「MVP 级动效纳入 P3，精细动效入 P6/后续」。其余按你规划执行，视觉/交互规范文本我会尽快给你。

—— haochen（产品）
